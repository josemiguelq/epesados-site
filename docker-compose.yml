version: "2"
services:
  node:
    container_name: site.epesados.prod
    build:
      context: ./
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    networks:
      - epesados
    ports:
      - "7778:80"
networks:
  epesados:
    external: true
